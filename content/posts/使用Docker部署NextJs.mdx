---
title: 使用Docker部署NextJs应用
slug: deploy-nextjs-application-using-docker
description: NextJS 官方提供了多种部署方式，包括：Vercel 部署（最简单）、Docker 容器化部署（最灵活，适合自托管）、静态导出部署、传统服务器部署
tag: Docker NextJs
date: 2025-11-04
---

```ts {.line-numbers}
const a: number = 1;
const b: number = 2;

function add(a: number, b: number): number {
  return a + b;
}

console.log(add(a, b));
```

# 使用 Docker 部署 NextJs 应用

## 使用 Docker 部署 NextJs 应用

### 使用 Docker 部署 NextJs 应用

#### 使用 Docker 部署 NextJs 应用

##### 使用 Docker 部署 NextJs 应用

###### 使用 Docker 部署 NextJs 应用

> 这是一个引用

这一最佳实践主要围绕如何改进异步编程的开发体验而展开。在后续的章节中大家可以自行感受。我也会在后续的实践案例中弱化对 useEffect 的使用。

例如，当我想要实现如下效果时。

在项目开发中，新页面渲染时请求一个接口的场景非常常见。新的架构思维的开发代码如下所示。

需要注意的是，本案例中的 api 都使用的是模拟接口，许多数据无任何业务描述，仅用于呈现 UI 的显示和变化。因此写法会与实际开发过程中稍有差异。
